<?xml version="1.0"?>
<project name="helloworld" default="run">
	<property name="targetdir" value="C:\test" />
	<target name="clean">
		<echo message="sadfsdf"/>
		<delete dir="${targetdir}" />
		<mkdir dir="${targetdir}" />
		
	</target>
	
	<!-- ================================= 
          target: run              
         ================================= -->
    <target name="run" depends="clean" description="description">
    	<echo message="run"/>
    </target>

</project>